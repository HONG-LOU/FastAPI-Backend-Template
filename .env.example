# 应用基本信息
APP_NAME=Career Fair API
APP_ENV=dev

# 安全相关（必填）
# 请替换为足够复杂的随机字符串，例如 64+ 位
SECRET_KEY=...

# JWT 配置
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_MINUTES=10080

# 数据库连接（必填）
# PostgreSQL async DSN 示例：postgresql+asyncpg://user:pass@host:port/db
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/career_fair

# CORS 允许的来源
# 支持逗号分隔字符串或 JSON 数组字符串
# 例如：http://localhost:3000,http://127.0.0.1:3000
# 或者： ["http://localhost:3000", "http://127.0.0.1:3000"]
BACKEND_CORS_ORIGINS=http://192.168.0.61:3000,http://localhost:3000

# 日志
# LOG_FORMAT: json | console（开发推荐 console，生产推荐 json）
LOG_LEVEL=INFO
LOG_FORMAT=console
LOG_JSON_PRETTY=true

